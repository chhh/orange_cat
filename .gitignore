# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# captured frames, event logs, background models
frame.jpg
frame_*.jpg
frames/
*.npy
*.csv

# sample clips and contact sheets (too big; see AGENTS.md §8)
samples/
cat-videos.zip
*.mp4
*.zip

# ffmpeg segment buffer if TMPDIR fallback lands in the repo
ocp-segments/

# editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# detector weights (13MB, regenerate: see AGENTS.md)
models/

# labelled clip sets pulled off Protect (214MB); see AGENTS.md §8
ooo/

# backups and editor leftovers
*.bak*
*.pre-git

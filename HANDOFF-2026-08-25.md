# Handoff to the Claude session on odd-fellow — 2026-08-25 14:10

Written by the session on Dave's laptop (`asset14201`), which drove today's
migration over SSH and will disappear when that laptop is powered down for its
fan repair. Everything here is state you cannot reconstruct from the repo or
the memory store.

## What happened today

The detector moved off Dave's laptop onto this box at **12:57**. The laptop's
`wg-quick@wg0` is stopped **and disabled**; this box holds `192.168.7.4`.
Never let both hold it — same key, same IP, and the UDM tracks one endpoint
per peer. Full writeup is in `second-box-zyxx.md` (this machine is the one
that document calls "zyxx").

**This box froze hard at 13:07**, an hour after setup — no cursor, off the
network, journal ending mid-second with no panic or thermal trip. It was power
cycled at ~13:10. In response, `root-setup-3.sh` added: `ocp-detector.service`
(systemd, `Restart=always`), `wg-quick@wg0` enabled, and the `iTCO_wdt`
hardware watchdog at `RuntimeWatchdogSec=60`.

**Use `systemctl` for the server now, not `./run-server.sh start`** — the old
script would launch a second, competing instance. `./run-server.sh status` is
still a fine health read.

## The open problem: 82 minutes of silence

**No motion event has ever landed on this box.** `frames/events.csv` does not
exist. Zero `POST /motion` lines since the server started.

The last POST *anywhere* was on the laptop at **12:43:28**. As of 14:05 that
is 82 minutes. For calibration, from the laptop's own history:

- 2026-08-25 had 58 events, median gap 5.7 min, 14 gaps over 15 min, max 61.5.
- The 13:00 hour normally carries 4/14/2/8/6 events on 08-20..08-24. Today: 0.
- But today ran at roughly half normal volume *before* the move — the 12:00
  hour had only 2 events while the laptop was demonstrably working.

So: past anything observed, but on a day that was already quiet. Unusual, not
proof.

## What is already ruled out — do not re-test these

- **Tunnel is up and bidirectional.** Handshake seconds old; `wg show` reports
  **30.59 MiB received** against 2.23 MiB sent — the inbound RTSP streams.
- **Not a firewall.** ufw reports `Status: inactive` on both machines. (An
  earlier claim that ufw was active here was wrong: `systemctl is-active ufw`
  returns `active` whenever the unit is loaded, even when the firewall is off.)
- **Not a log-pattern mistake.** Real POSTs appear as
  `192.168.1.133:xxxxx - "POST /motion HTTP/1.1" 200 OK`; the laptop logged
  3,758 of them.
- **Not the detector.** `uv run evaluate.py` returned **30/30** on this box —
  5/5 orange-intruder, 24/24 our-cats, 1/1 possum — matching the laptop
  exactly. The server is actively processing frames (background-refresh
  `relearning` lines prove the RTSP path works).
- **Not a recurring freeze.** `acpi_os_execute_deferred hogged` count is still
  **0** at 55 minutes uptime. On the boot that froze it was climbing by minute
  14 and hit 259 by 12:52. Keep watching it:
  `journalctl -b -k | /bin/grep -c "acpi_os_execute_deferred hogged"`

## What remains untested, and why it is blocked

Whether **Home Assistant is still firing** its motion automation, and if so
whether its POSTs reach port 8080. Those have different fixes and nothing on
this end distinguishes them.

The HA UI at `http://192.168.1.133:8123` routes fine through the tunnel but
**Dave has no credentials — it is Dima's instance.** So this check is blocked
on Dima, who has been asked whether the automation is still firing and whether
it shows errors since ~12:50. There was a genuine ~25 minute outage on our end
(12:53–13:17); failures inside that window are expected, failures *after* it
are the signal.

## Your job

1. **Watch for the first `POST /motion`.** That single event validates
   HA → tunnel → server → RTSP end to end, which nothing else can. Report it
   the moment it lands.
2. **If Dima confirms HA is firing normally and we still see nothing**, that is
   a real fault — say so clearly, because Dave was holding the laptop back
   specifically as a fallback for that case.
3. **Watch the ACPI counter** as described above.
4. **The first person event is also awaited.** The person-suppression fix
   shipped this morning in `9dabcd5` and has never been confirmed in the field;
   a person should now log a non-orange verdict.

## Memory

**You are the authoritative memory writer for this project from now on.** The
laptop's store freezes when it powers off; yours does not. See the
`memory-lives-on-odd-fellow` memory — when the laptop returns, its copy gets
overwritten from yours, never merged back.

Your memory store was last synced at ~13:45 (21 files). If you started before
then, re-read it.

## Access

Dave's laptop reaches this box as `david@192.168.1.254` with
`~/.ssh/ocp_handover`, a dedicated passphrase-less key. **Do not revoke it
yet** — it is what makes the memory sync possible when the laptop returns.
`sudo` cannot be driven over non-interactive SSH, so every root step today was
a script Dave ran at this box's terminal (`root-setup-1.sh`, `-2.sh`, `-3.sh`,
all still in `/home/david/`).

## When the laptop comes back (in ~1 day)

**The detector does not move back.** odd-fellow is permanent. The laptop
returns as a dev machine. Do not start `wg-quick@wg0` or the detector on it —
same key, same `192.168.7.4`, one endpoint per peer on the UDM. Full procedure
is in the `laptop-return-procedure` memory: pull memory across with rsync, add
`oddfellow` as a git remote and fetch (this box runs sshd now, so no more
bundles), and only then consider revoking the handover key. The laptop's
`frames/` is a dead archive ending 12:43 on 2026-08-25 — not current data.
